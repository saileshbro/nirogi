import 'dart:io';

import 'package:flutter/material.dart';

class ModifyImage extends StatelessWidget {
  final File image;
  const ModifyImage({
    @required this.image,
  });
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
